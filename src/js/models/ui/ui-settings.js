import Ui from "./ui";

class UiSettings extends Ui {
    constructor() {
        super("#app_settings");
    }

    setupDOM() {
        super.setupDOM();
    }

    setupEvents() {
        super.setupEvents();
    }
}

export default UiSettings;