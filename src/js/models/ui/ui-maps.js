import Ui from "./ui";

class UiMaps extends Ui {
    constructor() {
        super("#app_maps");
    }

    setupDOM() {
        super.setupDOM();
    }

    setupEvents() {
        super.setupEvents();
    }
}

export default UiMaps;