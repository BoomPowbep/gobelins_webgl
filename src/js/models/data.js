import DataManager from "./data/data-manager";

const DATA = {
    data_manager : null,
    ui_manager : null
};

//Create window.DATA to be able to debug data easily
window.DATA = DATA;

export default DATA;