import PickableList from "./pickable-list";

//For now, no override, only a specific class. If we need custom functions
class InstagramPostList extends PickableList {
}

export default InstagramPostList;